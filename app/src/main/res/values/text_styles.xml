<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Estilo para TextInputEditText en modo solo lectura -->
    <style name="ReadOnlyEditTextStyle">
        <item name="android:textColor">#757575</item>  <!-- Color gris para texto -->
        <item name="android:background">@android:color/transparent</item>
        <item name="android:enabled">false</item>
        <item name="android:alpha">0.7</item>  <!-- Transparencia para efecto visual -->
    </style>
</resources>

